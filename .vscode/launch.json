{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Debug Frontend (Localhost)",
            "url": "http://localhost:5050",
            "webRoot": "${workspaceFolder}/wwwroot"
        }
    ]
}